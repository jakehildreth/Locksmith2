# Agent skills

## Issue tracker

Issues and specs live as local markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

## Triage labels

Using the default canonical label vocabulary. See `docs/agents/triage-labels.md`.

## Domain docs

Single-context repo — read `CONTEXT.md` at the repo root (if it exists) and ADRs in `Docs/ADR/`. See `docs/agents/domain.md`.
